import Button from "react-bootstrap/Button";

import "./CreateButton.css";

const CreateButton = () => {
  return (
    <Button variant="secondary" className="CreateButton">
      Create Button
    </Button>
  );
};

export default CreateButton;
